import { FIELD_FILTER_OPERATORS } from '@data/entity/field-filter.entity';

export enum INTEGRITY_FIELDTYPE {
    USER = 'USER',
    SHORTTEXT = 'SHORTTEXT',
    LONGTEXT = 'LONGTEXT',
    GROUP = 'GROUP',
    IBPL = 'IBPL',
    PROJECT = 'PROJECT',
    DATE = 'DATE',
    RELATIONSHIP = 'RELATIONSHIP',
    INTEGER = 'INTEGER',
    PICK = 'PICK',
    STATE = 'STATE',
    FVA = 'FVA',
}
export const FIELD_FILTER_TYPE_OPERATORS = {
    [INTEGRITY_FIELDTYPE.DATE]: [FIELD_FILTER_OPERATORS.BEFORE, FIELD_FILTER_OPERATORS.AFTER],
    [INTEGRITY_FIELDTYPE.PICK]: [FIELD_FILTER_OPERATORS.EQUAL, FIELD_FILTER_OPERATORS.NOT_EQUAL],
};

export const TEXT_TYPES: string[] = [INTEGRITY_FIELDTYPE.SHORTTEXT, INTEGRITY_FIELDTYPE.LONGTEXT, INTEGRITY_FIELDTYPE.FVA];
